# Lauryn's Submission

## Intro

Both assignment sections are placed within the same page, with Section A at the top, and Section B at the bottom.

## Run the project

1. Clone the repo and run `pnpm install` to install all dependencies using pnpm.
2. run `pnpm dev` to launch local development.
3. Visit https://kontinentalist-test-lauryn.netlify.app/ to view deployed site
